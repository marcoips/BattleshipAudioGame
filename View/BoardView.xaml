<UserControl x:Class="BattleshipAudioGame.View.BoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BattleshipAudioGame.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">



                     <!--   Recursos locais -->
    <UserControl.Resources>
        <!-- Template de uma célula 30×30 -->
        <DataTemplate x:Key="CellTemplate">
            <Border BorderBrush="DarkGray" BorderThickness="0.5"
                    Background="{Binding Background}"
                    Width="30" Height="30">
                <TextBlock Text="{Binding Content}"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Border>
        </DataTemplate>
    </UserControl.Resources>



    <Grid>

        <!-- Título -->
        <TextBlock Text="{Binding BoardTitle}" FontSize="18" FontWeight="Bold"
                   HorizontalAlignment="Center"/>

        <!-- Zona principal (rótulos + células) -->
        <Grid Margin="0,30,0,0">
            <!-- 11 colunas: 1 para rótulos + 10 para células -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 11 linhas: título já está, aqui só rótulos + células -->
            <Grid.RowDefinitions>
                <RowDefinition Height="24"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Rótulos das colunas (1‑10) -->
            <ItemsControl ItemsSource="{Binding ColumnLabels}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="1" Columns="10"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" HorizontalAlignment="Center"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.Margin>0,0,0,2</ItemsControl.Margin>
                <Grid.Column>1</Grid.Column>
                <Grid.Row>0</Grid.Row>
            </ItemsControl>

            <!-- Rótulos das linhas (A‑J) -->
            <ItemsControl ItemsSource="{Binding RowLabels}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="1" Rows="10"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.Margin>0,0,2,0</ItemsControl.Margin>
                <Grid.Column>0</Grid.Column>
                <Grid.Row>1</Grid.Row>
            </ItemsControl>

            <!-- Células 10×10 -->
            <ItemsControl ItemsSource="{Binding Cells}"
                          ItemTemplate="{StaticResource CellTemplate}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="10" Columns="10"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <!-- deslocado 1 linha/coluna por causa dos rótulos -->
                <Grid.Column>1</Grid.Column>
                <Grid.Row>1</Grid.Row>
            </ItemsControl>
        </Grid>
    </Grid>
</UserControl>
