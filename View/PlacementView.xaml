<UserControl x:Class="BattleshipAudioGame.View.PlacementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:view="clr-namespace:BattleshipAudioGame.View" Height="116">
    
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" >
        <view:BoardView DataContext="{Binding PlayerBoard}" />
        
        <TextBlock Text="Modo de Posicionamento" FontSize="24" TextAlignment="Center"/>
        
        <Button Content="Continuar (placeholder)" Width="180" Command="{Binding ContinueCommand}"/>
    
    </StackPanel>
</UserControl>